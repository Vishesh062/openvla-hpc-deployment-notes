# Save this file as `.gitignore` (with the dot at the front) in your repo root.
# GitHub will pick it up automatically.
# ---------------------------------------------------------------

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Python (in case any snippets get expanded)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Build artifacts
*.log
*.tmp

# Private archive — keep original (un-sanitised) reports out
_private/
*-original.pdf
*-original.docx
